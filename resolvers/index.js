const { Query } = require('./Query');
const { Category } = require('./Category');
const { Product } = require('./Product');
const { Mutation } = require('./Mutation');

module.exports = { Query, Category, Product, Mutation };
